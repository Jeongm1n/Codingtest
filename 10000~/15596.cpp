/*#include<iostream>
#include<vector>
using namespace std;
long sum(std::vector<int>& a) {
	int size = a.size();
	int sum = 0;
	for (int i = 0; i < size; i++)
		sum += a[i];
	return sum;
}
int main() {
	int n;
	cin >> n;
	vector<int> arr(n);
	for (int i = 0; i < n; i++)
		cin >> arr[i];

	cout << sum(arr) << endl;
	return 0;
}*/